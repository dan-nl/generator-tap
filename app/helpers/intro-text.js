'use strict';

/**
 * @type {string}
 */
var intro = [
  '',
  'this generator will run the following tasks:',
  '',
  '  * install tap and optionally sinon',
  ''
].join( '\n' );

module.exports = intro;
